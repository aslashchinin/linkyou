import 'package:flutter/material.dart';
import 'package:linkyou/models/photo.dart';

class UserPhotoTile extends StatelessWidget {
  const UserPhotoTile({super.key, required this.photo});

  final Photo photo;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
