name: linkyou
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_svg: ^2.0.10+1  
  http: ^1.2.1
  provider: ^6.0.0
  carousel_slider: ^4.0.0
  percent_indicator: ^4.2.2
  shared_preferences: ^2.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/welcome_people.svg
    - assets/welcome_people.png
